### Livekit Server

`cd backend`

`source venv/bin/activate`

`python3 agent.py dev`

### Unidirectional Agent (Activation Announcement)

For one-way activation announcements (no interaction):

`cd backend`

`source venv/bin/activate`

`python3 unidirectional_agent.py dev`

### Agent Backend Server

`cd backend`

`source venv/bin/activate`

`python3 server.py`
